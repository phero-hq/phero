{
  "name": "@samen/server",
  "version": "0.9.1",
  "main": "dist/index.js",
  "description": "Server library for Samen",
  "homepage": "https://samen.io",
  "repository": "samen-io/samen",
  "bugs": "https://github.com/samen-io/samen/issues",
  "engines": {
    "node": ">=12.22.0"
  },
  "bin": {
    "samen-server": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist && rm -rf generated",
    "build": "tsc",
    "test": "jest src",
    "prepublish": "npm run clean && npm run build",
    "postbuild": "cp package.json ./dist/package.json"
  },
  "dependencies": {
    "@samen/core": "^0.9.1",
    "@samen/dev": "^0.9.1"
  },
  "peerDependencies": {
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "Press Play Development B.V.",
    "email": "samen@pressplay.dev",
    "url": "https://pressplay.dev"
  }
}
