{
  "name": "@samen/client",
  "version": "0.8.0-alpha.6",
  "main": "package/index.js",
  "bin": {
    "samen-client": "dist/cli.js"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist && rm -rf generated",
    "build": "tsc",
    "test": "jest src",
    "prepublish": "npm run clean && npm run build",
    "postbuild": "cp package.json ./dist/package.json"
  },
  "dependencies": {
    "@samen/core": "^0.8.0-alpha.6",
    "@samen/dev": "^0.8.0-alpha.6",
    "typescript": "^4.4.4"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/jest": "^27.0.2",
    "@types/node": "^16.11.7",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7"
  }
}
