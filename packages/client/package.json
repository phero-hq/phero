{
  "name": "@samen/client",
  "version": "0.0.0",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build && tsc"
  },
  "dependencies": {
    "@samen/core": "^0.0.0"
  },
  "devDependencies": {
    "typescript": "^4.1.2"
  }
}
